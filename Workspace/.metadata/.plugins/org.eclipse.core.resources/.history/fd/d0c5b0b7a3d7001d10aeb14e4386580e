import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Main {
    
    public static void main(String[] args) throws IOException {
        
    	
        InputStreamReader ir = new InputStreamReader(System.in);
        BufferedReader leitor = new BufferedReader(ir);
        
        int par = 0;
        for (int i = 0; i < 5; i++) {
        	int A = Integer.parseInt(leitor.readLine());
        	if(A % 2 == 0) {
        		par ++;
        	}
		}
        
        int B = Integer.parseInt(leitor.readLine());
        int C = Integer.parseInt(leitor.readLine());
        int D = Integer.parseInt(leitor.readLine());
        int E = Integer.parseInt(leitor.readLine());
        
        
        
        System.out.println("SOMA = " + SOMA);
        
    }
    
}

